import React from 'react';

import { SpinnerStyles as S } from './spinner.styled';

export const Spinner = (): JSX.Element => {
  return (
    <S.Container>
      <S.Spinner />
    </S.Container>
  );
};
